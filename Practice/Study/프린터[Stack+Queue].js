function solution(priorities, location) {
    
    //Queue
    let check = false;
    var rotate = priorities;
    let tmp = [];
    for (let i = 0; i < priorities.length; i++) {
        let priority = priorities[i];
        let rotate = [];
        for(let j=i;j<priorities.length;j++){
            if(priorities[j]>priority){
            }else{
            }
        }

    }
    var answer = 0;
    return answer;
}

solution([2,1,3,2],2);

/**
 * 순서대로 -> Queue
 * 
 */